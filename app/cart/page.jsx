import ShoppingCart from '../../src/Components/ShoppingCart';
export const metadata = { title: 'Shopping Cart', robots: { index: false, follow: false } };

export default function CartPage() {
  return <ShoppingCart />;
}
