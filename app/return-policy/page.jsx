import ReturnPolicy from '../../src/Components/ReturnPolicy';
export const metadata = { title: 'Return Policy', alternates: { canonical: '/return-policy' } };

export default function ReturnPolicyPage() {
  return <ReturnPolicy />;
}
